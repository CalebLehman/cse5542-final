class Buffer {
    constructor(
        buffer,
        itemSize,
        numItems
    ) {
        this.buffer   = buffer;
        this.itemSize = itemSize;
        this.numItems = numItems;
    }
}

export { Buffer };
