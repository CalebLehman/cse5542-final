class Geometry {
    constructor(
        init,
        anim,
        get,
        texture
    ) {
        this.init    = init;
        this.anim    = anim;
        this.get     = get;
        this.texture = texture;
    }
}

export { Geometry };
